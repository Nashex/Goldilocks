package listeners;

import net.dv8tion.jda.api.hooks.ListenerAdapter;

public class DisconnectListener extends ListenerAdapter {
//    @Override
//    public void onGuildVoiceLeave(@Nonnull GuildVoiceLeaveEvent event) {
//        if (event.getMember().getUser().equals(Goldilocks.jda.getSelfUser())) {
//            GuildMusicManager guildMusicManager = PlayerManager.getInstance().getGuildMusicManager(event.getGuild());
//            guildMusicManager.player.stopTrack();
//        }
//    }
}
